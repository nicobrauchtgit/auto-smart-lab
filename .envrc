#!/usr/bin/env bash

# Automatically sets up the Devbox environment whenever this directory is
# entered through direnv. Secrets are loaded from the ignored .env file via
# devbox.json's env_from setting.
eval "$(devbox generate direnv --print-envrc)"
