Start a child with a separate context in the configured shared workspace. Supply
a bounded task with relevant context, paths, constraints, and expected results.
Returns a handle immediately. The child has no parent conversation history.
