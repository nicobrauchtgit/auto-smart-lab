{
  "name": "auto-smart-lab",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "fetch-unit": "bun agent/setup/fetch_unit_cli.ts",
    "pipeline": "bun agent/pipeline_cli.ts",
    "research": "bun agent/research_cli.ts",
    "test:py": "\"${VENV_DIR:-$PWD/.venv}/bin/python\" -m unittest discover -s agent/solve/tests -t agent && \"${VENV_DIR:-$PWD/.venv}/bin/python\" -m unittest discover -s agent/setup/tests",
    "solve": "bun agent/run/orchestrate.ts",
    "pi:hi": "bun agent/pi_sdk.ts",
    "traces": "next dev agent/dashboard -p 3001",
    "test": "bun test",
    "solve-stage": "bun agent/solve_cli.ts"
  },
  "dependencies": {
    "@earendil-works/pi-ai": "0.84.1",
    "@earendil-works/pi-coding-agent": "0.84.1",
    "next": "^16.3.2",
    "postgres": "^3.4.9",
    "react": "^19.2.8",
    "react-dom": "^19.2.8"
  },
  "devDependencies": {
    "@tailwindcss/postcss": "^4.3.3",
    "tailwindcss": "^4.3.3"
  }
}
